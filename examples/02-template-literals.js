/*
  Bridge Front-end Development
  ES6 & modern JavaScript

  ES6 String Concatenting with Template literals Example

  Run this Example by running the following in your terminal:
    npm run example template-literals
*/

const name = "Purvi";
const job = "CTO";
const company = "Bridge";

const sentence = `Hi, I'm ${name}! I work for ${company} as the ${job}`;

console.log(sentence); // "Hi, I'm Purvi! I work for Bridge as the CTO"
