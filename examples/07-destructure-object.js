/*
  Bridge Front-end Development
  ES6 & modern JavaScript

  ES6 get properties from an object with destructuring Example

  Run this Example by running the following in your terminal:
    npm run example destructure-object
*/

const cat = { name: "Carrot", weight: 20 };
const { name, weight } = cat;
console.log(name, weight); // Carrot 20
